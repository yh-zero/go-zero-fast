syntax = "v1"

import (
	"user_struct.api"
)

@server (
	prefix: /v1/sys
	group:  usernoauth
)
service Ysy {
	@doc "系统用户登录"
	@handler Login
	post /user/login (LoginReq) returns (LoginRes)

	@doc "获取验证码"
	@handler getCaptcha
	get /captcha returns (CaptchaRes)
}

