Name: Ysy
Host: 0.0.0.0
Port: 6100

SysRPC:
  Etcd:
    Hosts:
      - 127.0.0.1:2379
    Key: sys.rpc
  NonBlock: true

JwtAuth:
  AccessSecret: sD5fG2hJkL9pQw3R-go-zero-fast
  AccessExpire: 864000
#  AccessExpire: 64800

BizRedis:
  Host: 127.0.0.1:6379
  Pass:
  Type: node

DB:
#  DataSource: root:123456@tcp(127.0.0.1:3306)/goZero-admin?charset=utf8mb4&parseTime=True&loc=Asia%2FShanghai
  DataSource: root:123456@tcp(127.0.0.1:3306)/go-zero-fast?charset=utf8mb4&parseTime=True&loc=Asia%2FShanghai
  MaxIdleConns: 10
  MaxOpenConns: 100
  ConnMaxLifetime: 3600

CasbinConf:
  ModelText: |
    [request_definition]
    r = sub, obj, act
    [policy_definition]
    p = sub, obj, act
    [role_definition]
    g = _, _
    [policy_effect]
    e = some(where (p.eft == allow))
    [matchers]
    m = r.sub == p.sub && myFun(r.obj,p.obj) && r.act == p.act
#    m = r.sub == p.sub && r.obj == p.obj && r.act == p.act

# 是否开启验证码
IsCaptcha: false